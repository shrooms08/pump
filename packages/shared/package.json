{
  "name": "@pump/shared",
  "version": "0.0.0",
  "private": true,
  "description": "Seed RNG, game constants, and wire-protocol types shared by the PUMP client and server.",
  "type": "module",
  "main": "./src/index.ts",
  "types": "./src/index.ts",
  "exports": {
    ".": "./src/index.ts",
    "./rng": "./src/rng.ts",
    "./constants": "./src/constants.ts",
    "./types": "./src/types.ts"
  },
  "scripts": {
    "build": "tsc -p tsconfig.build.json",
    "typecheck": "tsc -p tsconfig.json",
    "test": "vitest run"
  },
  "devDependencies": {
    "typescript": "^5.5.4",
    "vitest": "^2.1.9"
  }
}
